#include <iostream>
#include <algorithm>

using namespace std;

int main()
{

    int n;
    cin >> n;

    int arr[100000];
    int i = 0;
    while (n != 0)
    {
        int x;
        cin >> x;
        if (x ==)
            if ()
                cin >> arr[i];
        n--;
        i++;
    }
    int *num = arr;
    num = new int[i];

    return 0;
}
